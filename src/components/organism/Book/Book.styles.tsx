import styled from "styled-components";

export const Wrapper = styled.article`
  display: flex;
  flex-direction: column;
  gap: 20px;
  padding-left: 10px;
  header {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  p {
    margin: 0 0 10px;
  }
`;
